<label>ジャンル編集</label>
<br><br>

<%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f| %>
  <label>ジャンル名</lab>
    <%= f.text_field :name %>
    <%= f.submit '変更を保存' %>
<% end %>