<div class="container px-sm-0 pt-4 row">
  <div class=" col-2 h-100 border border-dark mt-3 ml-5 p-0">
    <div class="border-bottom border-dark">
      <div class="py-1 text-center">ジャンル検索</div>
    </div>
    <div class="py-3 px-4">
      <% @genres.each do |genre| %>
        <div class="py-1"><%= genre.name %></div>
      <% end %>
    </div>
  </div>
  <div class="col-9 mt-3 mx-auto">
    <div class="col-12 bg-warning bg-opacity-25 py-5 px-5">
      <div class="bg-white text-center py-5 px-2">
        <p class="mb-0">
          ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。
        </p>
      </div>
    </div>
    <div>
      <h2 class="py-4">
        <b>新着商品</b>
      </h2>
      <div>
        <% @items.each do |item| %>
          <div><%= image_tag item.get_image(300,300) %></div>
          <div><b><%= item.name %></b></div>
          <div><b>¥<%= ApplicationHelper.tax_price(item.price) %></b></div>
        <% end %>
      </div>
      <div class="text-right mb-3">
          <u><%= link_to '全ての商品を見る >',public_items_path %></u>
      </div>
    </div>
  </div>

</div>